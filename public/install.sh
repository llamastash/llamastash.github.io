#!/bin/sh
# Placeholder install.sh — no release yet.
#
# This file is replaced on every tagged release by .github/workflows/bump.yml
# which fetches the verified install.sh asset from the GH Release.
#
# Until the first release lands, follow:
#   https://github.com/llamastash/llamastash
echo "LlamaStash: no release yet — see https://github.com/llamastash/llamastash" >&2
exit 1
